{
  "name": "cloudflare-worker-boilerplate",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready Cloudflare Worker TypeScript boilerplate.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "wrangler types && tsc --noEmit",
    "test": "vitest",
    "check": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@cloudflare/workers-types": "^4.20260514.1",
    "typescript": "^5.5.0",
    "vitest": "2.1.9",
    "wrangler": "^3.78.0"
  }
}
